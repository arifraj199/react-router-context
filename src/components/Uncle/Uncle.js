import React from 'react';

const Uncle = ({house}) => {
    return (
        <div>
            <h2>This is Uncle</h2>
            <h3>House:{house}</h3>
        </div>
    );
};

export default Uncle;